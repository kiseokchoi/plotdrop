# Third-party notices

Plotdrop uses third-party open-source software. The principal runtime components include:

- React and React DOM — MIT License
- Tauri and the Tauri API — Apache-2.0 OR MIT
- Tauri dialog and filesystem plugins — Apache-2.0 OR MIT

The complete JavaScript and Rust dependency graphs are recorded in `package-lock.json` and `src-tauri/Cargo.lock`. Each dependency remains subject to its own license and copyright notices. This file is informational and does not replace those licenses.

